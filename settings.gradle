rootProject.name = 'dmtools-cli'
include 'dmtools-core', 'dmtools-mcp-annotations', 'dmtools-annotation-processor'
def mermaidRendererBuild = new File(settingsDir, 'dmtools-mermaid-renderer')
if (mermaidRendererBuild.isDirectory()) {
    includeBuild mermaidRendererBuild
}
